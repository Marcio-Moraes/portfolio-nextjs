
type ListaIconesProps = {
    id: number;
    nomeIcon: string;
    img: string;
}

export const ListaIcones = ({}: ListaIconesProps) => {
    return(
        <ul className="">
            <li>...</li>
        </ul>
    )
}